#include<stdio.h>

int main()
{
    int a[2][2]={1,2,3,4}, i, j, sum;
    sum = 0;
    for(i=0; i<; i++)
    {
        for(j=0; j<; j++)
        {
            sum = sum + a[i][j];
        }
    }
    printf("\nSum of all elements = %d", sum);
  
    return 0;
}